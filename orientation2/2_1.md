(W) a) Why does an arrival event need to generate a new arrival event? b) Which event should be generated by the start of the service (of a single service point)?


- A) The simulation must continue producing future customers. When one customer arrives, the next arrival must be scheduled so the event list keeps the system running over time.
- B) The start of service should generate a service completion / exit event.